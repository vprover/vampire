% Nothing makes these constants distinct, so this is not a theorem. The parser
% used to register a distinct group for it and report Theorem, even though the
% conjecture had already been negated by then.

fof(c, conjecture,
    $distinct(a,b,c,d,e,f) ).
